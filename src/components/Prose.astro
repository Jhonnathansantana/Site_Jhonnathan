---
export interface Props {
  class?: string;
}

const { class: extraClass = '' } = Astro.props;
---

<div
  class={`prose prose-slate max-w-none dark:prose-invert prose-headings:font-semibold prose-a:text-brand-600 hover:prose-a:text-brand-700 dark:prose-a:text-brand-400 dark:hover:prose-a:text-brand-300 ${extraClass}`}
>
  <slot />
</div>
